# Doctor Appointment Backend

ENV:
MONGO_URI=...
JWT_SECRET=...

Clone the repository
git clone https://github.com/errajput/careHub-api.git
cd CareHub-API

Install & Run:
npm install
npm run dev

Endpoints prefixed with /api as described in the assignment.
